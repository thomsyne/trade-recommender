# Phase 6A bounded closure verification receipt

Date: 2026-09-11. Scope: R1–R3 only. Parent corrected candidate commit/tree:
`ce089dbb2813dff5574d50c20d4eabbec2ddf3df` /
`943a9e568c3290ae6a4926f74ae0aa5a5c10413d`.

## Focused PostgreSQL 15 evidence

- Server: PostgreSQL `15.19 (Debian 15.19-0+deb12u1)`.
- `POSTGRES_CONN_MAX_AGE=0 .venv/bin/python manage.py test assessments.tests.test_engine assessments.tests.test_migrations --verbosity 1`
  — 21 discriminating engine/migration tests passed.
- `POSTGRES_CONN_MAX_AGE=0 .venv/bin/python manage.py test assessments.tests --verbosity 1`
  — 37 focused Phase 6A tests passed, including prior closed-boundary regressions.
- A genuine v1 assessment was generated by executing the engine source from the
  original candidate commit under migration `0002`; after `0003`/`0004`, replay
  was byte-equivalent and the read-only audit was clean. New v1 admission failed.
- A hash-consistent wrong-era/wrong-provenance eligibility inserted under `0002`
  survived forward migration but failed latest Python and raw-SQL assessment
  admission. Canonical-empty SQL/Python output parity passed.
- Real Phase 5 setup construction verified exact boundary, one-microsecond and
  seconds-delayed availability, and Friday-to-Sunday registered M15 succession.
- Local `0003 → 0004` migration passed. The explicit read-only audit returned
  `{"checked":0,"has_more":false,"next_after_id":0,"violations":[]}`.

PostgreSQL 17 is unavailable in this orb: only PostgreSQL 15 is installed and the
Docker client has no daemon socket. No substitute version claim is made.

## Static, drift, and scope evidence

- `make check` passed: Ruff clean, all 585 files formatted, Django system check
  clean, no migration drift, and compileall clean.
- Method implementation pin, method digest, and migration pin agree at the exact
  identities frozen in `closure-source-manifest.json`.
- `git diff --check` passed. Applied migrations `0001`–`0003`, active-stack
  source/governance pins, and `forecasts`, `operations`, `market`, `research`,
  `config`, and `Makefile` are byte-unchanged from the parent candidate.
- Exact closure source hashes are frozen in `closure-source-manifest.json`; both
  preceding manifests and receipts remain unchanged and are checked against their
  historical commits.

No broad suite was run. No provider/model was called and nothing was pushed,
deployed, activated, scheduled, merged, opened as a pull request, or written to
Phase 5.5 resources. This receipt is engineering evidence, not independent
acceptance or permission to trade.
