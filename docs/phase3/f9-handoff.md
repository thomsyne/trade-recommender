# F9 current-risk correction

Engineer disposition: ready for bounded independent F9 re-review; NO-SHIP pending
that review and the subsequent PM stage. This does not reopen the accepted F1–F8
findings and does not assert that the entire broad suite is green.

The prior Exposure population selected historical admission plus no paper result.
Source-only terminal facts therefore retained C$125 each in displayed risk even
though portfolio capacity excluded them. Exposure selection, direct report
iterables and portfolio capacity now call `current_risk_projection`: a qualifying
v2/v3/v4 directional recommendation needs effective admission and a factual
`admitted_awaiting_entry` or `entered` projection. Each request uses one cutoff.
Future results do not exclude earlier risk. The projection refreshes immutable
facts, so passing a stale recommendation cannot restore terminal risk. Legacy
terminal source facts take precedence over an older recorded entry. No sizing
amount, cap, execution geometry, immutable record, migration or constraint changed.

All evidence is in
`/Users/oluwatomisintaiwo/trade-recommender-artifacts/phase3-f9-_cj9f2v_`.
`commands.jsonl` records exact commands, sanitized owned database environment,
return codes and outputs. Parent of this correction is
`d02dc1a3bad8bcff0270b9ac2176d3098aea09f8`.

| Evidence | Result and boundary |
|---|---|
| `f9-before.log` | Unchanged independent six-test repro on d02dc1: four failures (three source-only terminals and exact cancellation cutoff); two positives pass and pre-cancellation assertion passes. |
| `regressions-before.log` | New demanding cases before owning fix: seven failures plus one erroneous revocation fixture, with four imported helper tests also discovered. The fixture used a nonexistent idempotency field. The next log preserves its missing reason-code correction; neither is an owning product failure. Helper import was moved inside setUp to avoid duplicate discovery. |
| `adjacent.log` | 49 tests pass: nine new current-risk cases, six unchanged independent F9 probes, exposure arithmetic, portfolio selection/concurrency, dashboard, reviews and execution neighbors. No assertions were weakened. |
| `legacy-bootstrap.log`, `legacy-seed.log`, `legacy-before.json`, `legacy-upgrade.log`, `legacy-check.log`, `legacy-after.json` | Untouched base 16a2702 generated a real v3 recommendation, real sizing/admission and actual resolver entry under a separate fixture policy activated before issuance. A synthetic local owner cancellation was recorded as a legacy source fact before cutover. Normal upgrade preserves facts: admitted125 → entered125 → cancelled0, and pre-cancellation125. No v4 downgrade or invented backfill. |
| `legacy-result.log`, `legacy-result-before.json`, `legacy-result-after.json` | A second fresh base database independently generates real admission, resolver entry and a resolver-produced target result from the subsequent executable bid candle. Normal upgrade preserves admitted125 → entered125 → target_hit0, including immediately before result125. This terminal proof does not rely on manual lifecycle fabrication. |
| `ui-matrix.json`, `capture.log`, `paper-dom.txt`, `paper.png`, `exposure-dom.txt`, `exposure.png` | Actual running app, synthetic owner session, isolated headless Chrome with non-local requests blocked. Both HTTP200. Paper: five factual rows including three source-only terminals, one awaiting entry and one entered. Exposure: precisely the two active IDs, each C$125, total C$250, matching portfolio. Both PNGs visually inspected; no template changes. |

The new `CurrentRiskBoundaryTests` cover admitted/entered positives; cancellation,
revocation, entered missing_data and expired_unobserved; target, invalidation,
no-activation expiry and after-entry expiry. Every terminal case checks one
microsecond before, exactly at and immediately after, both selected and explicitly
passed stale rows. Result facts are generated by the actual paper resolver from
real synthetic candles; costs and adverse precedence remain unchanged.

UI fixture provenance: the first second-target entry used a duplicate ingestion
manifest and correctly reused an earlier run, so no entry occurred. The diagnostic
and failed seed log are preserved. A distinct manifest supplied the actual second
target hourly evidence; no execution rule was modified to obtain the screenshot.

Final broad result: 310 tests in 191.861 seconds, zero failures and 19 errors.
There are zero branch-only and nine base-only failing identities. All 19 shared
identities retain their explicitly different causes. Cleanup is recorded in the
final artifact manifest. The
retained independent base evidence is 222 tests / 5 failures / 23 errors at 16a2702.
The previous branch run was 301 tests / 19 errors. The 19 shared Gate8 identities
have different causes: base reverses market0027 and rejects a registration
validator body, while branch encounters the truthful irreversible forecasts0030
dependency. They are not claimed to be the same cause or a green suite. The exact
final F9 broad command uses the same labels and flags; normalized identities,
counts and causes are preserved in `normalized-differential.json`.

Only fresh owned UTF8 PostgreSQL databases and the documented market0027 fake
bootstrap were used. No provider, production, infrastructure, push, secret or
unowned resource access was performed. Final commit, source hashes, owned app/PG
shutdown and exact-directory cleanup are recorded in `final-manifest.json`.
