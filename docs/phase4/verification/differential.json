{
  "command": "manage.py test market operations forecasts --keepdb --noinput -v 2",
  "base_ref": "a3fbe6ce7fc08895c5696a2a25744f46e5d6e284",
  "starting_ref": "064f98ac565b6d071eac0030ff477f9919333cde",
  "database_bootstrap": "Separate UTF8/template0 DBs: market0026; fake only0027; normal migrate.",
  "base": {
    "tests": 838,
    "seconds": 1070.772,
    "result": "FAILED (failures=2, errors=244)",
    "log_sha256": "d2de4e4288419c0b03b4d061211cdc3dc0c5d60b98e58737b2df958023f50dbd",
    "identities": {
      "test_0029_applies_over_legacy_and_cross_source_chains_and_renumbers_dense (market.tests.test_zzzzzzzz_live_observation_migration.LineageRenumberMigrationTests.test_0029_applies_over_legacy_and_cross_source_chains_and_renumbers_dense)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_0029_migration_depends_on_0028 (market.tests.test_zzzzzzzz_live_observation_migration.LineageRenumberMigrationTests.test_0029_migration_depends_on_0028)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_canary_with_too_few_earlier_observations_blocks_progression (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_canary_with_too_few_earlier_observations_blocks_progression)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_complete_but_wrong_successor_outcome_is_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_complete_but_wrong_successor_outcome_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_complete_plan_permits_nothing_and_reports_successful_completion (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_complete_plan_permits_nothing_and_reports_successful_completion)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_attempt_on_another_plan_does_not_block_the_successor (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_attempt_on_another_plan_does_not_block_the_successor)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_attempt_permits_nothing_and_reports_the_block (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_failed_attempt_permits_nothing_and_reports_the_block)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_canary_blocks_all_progression (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_failed_canary_blocks_all_progression)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_canary_blocks_every_other_successor_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_canary_blocks_every_other_successor_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_cross_instrument_attempt_blocks_the_rest (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_cross_instrument_attempt_blocks_the_rest)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_remainder_attempt_blocks_a_different_remainder_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_remainder_attempt_blocks_a_different_remainder_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_fresh_materialized_plan_exposes_only_the_canary (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_fresh_materialized_plan_exposes_only_the_canary)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_missing_chunk_fails_at_commit (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_missing_chunk_fails_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_partial_successor_marker_is_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_partial_successor_marker_is_refused)\nA row may not wear the successor identity without earning it, and it": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_partial_successor_outcome_is_refused_before_the_aggregates (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_partial_successor_outcome_is_refused_before_the_aggregates)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_predecessor_failure_does_not_alter_successor_readiness (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_predecessor_failure_does_not_alter_successor_readiness)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_predecessor_inventory_does_not_satisfy_successor_completeness (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_predecessor_inventory_does_not_satisfy_successor_completeness)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_quarantined_attempt_is_blocked_and_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_quarantined_attempt_is_blocked_and_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_row_declared_approval_cannot_seal_without_the_reconstruction (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_row_declared_approval_cannot_seal_without_the_reconstruction)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_running_attempt_is_never_reported_as_available (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_running_attempt_is_never_reported_as_available)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_second_attempt_is_never_reported_as_eligible (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_second_attempt_is_never_reported_as_eligible)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_second_attempt_one_is_impossible (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_second_attempt_one_is_impossible)\n18, 19: attempt 1 is unique per chunk at the database level.": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_succeeded_attempt_does_not_trigger_the_failed_plan_rule (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_succeeded_attempt_does_not_trigger_the_failed_plan_rule)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_successor_plan_short_of_its_declared_count_is_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_successor_plan_short_of_its_declared_count_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_valid_canary_opens_exactly_the_five_cross_instrument_chunks (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_valid_canary_opens_exactly_the_five_cross_instrument_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_131_remaining_chunks_permit_attempt_one_only (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_all_131_remaining_chunks_permit_attempt_one_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_remaining_chunks_eligible_and_canary_permanently_closed (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_all_remaining_chunks_eligible_and_canary_permanently_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_succeeded_but_one_inventory_missing_is_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_all_succeeded_but_one_inventory_missing_is_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_altered_governance_is_rejected_by_preflight (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_altered_governance_is_rejected_by_preflight)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_an_exhausted_stage_short_of_completion_permits_nothing (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_an_exhausted_stage_short_of_completion_permits_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_an_inventory_bound_to_another_attempt_does_not_satisfy_the_chunk (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_an_inventory_bound_to_another_attempt_does_not_satisfy_the_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_any_plan_evidence_blocks_reverse (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_any_plan_evidence_blocks_reverse)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_adds_no_object_and_changes_only_the_two_bodies (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_application_adds_no_object_and_changes_only_the_two_bodies)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_failure_commits_exactly_two_scope_events (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_application_failure_commits_exactly_two_scope_events)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_success_commits_exactly_two_scope_events (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_application_success_commits_exactly_two_scope_events)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_applied_0011_is_upgraded_to_operation_aware_historical_enforcement (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_applied_0011_is_upgraded_to_operation_aware_historical_enforcement)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_artifact_bytes_dry_run_readiness_and_database_inventory_enforcement (market.tests.test_gate7c_dataset_registration.Gate7CRegistrationTests.test_artifact_bytes_dry_run_readiness_and_database_inventory_enforcement)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_one_ledger_matches_the_governing_finding (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_one_ledger_matches_the_governing_finding)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_three_and_retry_are_rejected_after_failure (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_three_and_retry_are_rejected_after_failure)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_three_and_retry_are_rejected_after_success (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_three_and_retry_are_rejected_after_success)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_and_failed_evidence_are_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_attempt_two_and_failed_evidence_are_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_is_refused_through_the_service_and_raw_sql (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_attempt_two_is_refused_through_the_service_and_raw_sql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_or_wrong_key_rejected_for_fresh_chunks (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_attempt_two_or_wrong_key_rejected_for_fresh_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_audit_evidence_cardinality_enforced_in_postgresql (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_audit_evidence_cardinality_enforced_in_postgresql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_is_permanently_closed (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_canary_is_permanently_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_prerequisite_reconstructs_and_forgeries_fail (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_canary_prerequisite_reconstructs_and_forgeries_fail)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_success_exposes_the_five_cross_instrument_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_canary_success_exposes_the_five_cross_instrument_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_success_prerequisite_is_established (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_canary_success_prerequisite_is_established)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_candle_conflict_quarantine_rolls_back_and_ends_failed (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_candle_conflict_quarantine_rolls_back_and_ends_failed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_checkpoints_contain_no_secrets_or_prices (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_checkpoints_contain_no_secrets_or_prices)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_execute_fails_closed_without_the_complete_state (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_execute_fails_closed_without_the_complete_state)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_rejects_wrong_plan_sha (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_rejects_wrong_plan_sha)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_requires_an_existing_django_approver (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_requires_an_existing_django_approver)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_success_stores_exactly_the_sealed_series_once (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_command_success_stores_exactly_the_sealed_series_once)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_verifies_the_artifact_byte_for_byte (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_verifies_the_artifact_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_verifies_the_completion_summary_byte_for_byte (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_verifies_the_completion_summary_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_committed_artifacts_reconstruct_and_reject_alteration (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_committed_artifacts_reconstruct_and_reject_alteration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_committed_authorization_artifact_matches_the_governed_pins (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_committed_authorization_artifact_matches_the_governed_pins)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_complete_but_nonmatching_state_is_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_complete_but_nonmatching_state_is_rejected_at_both_layers)\nAll 132 inventories exist, but synthetic observations cannot match": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_complete_coverage_reaches_registration_boundary_unregistered (market.tests.test_gate7b_full_acquisition.Gate7BFullCoverageTests.test_complete_coverage_reaches_registration_boundary_unregistered)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_approval_attempts_serialize_and_leave_nothing (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_concurrent_approval_attempts_serialize_and_leave_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_sql_starts_commit_exactly_one_attempt (market.tests.test_gate7a_acquisition_canary.Gate7AConcurrencyTests.test_concurrent_raw_sql_starts_commit_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_sql_starts_yield_one_winner (market.tests.test_gate7b_full_acquisition.Gate7BConcurrencyTests.test_concurrent_raw_sql_starts_yield_one_winner)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_starts_permit_exactly_one_running_attempt (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_concurrent_raw_starts_permit_exactly_one_running_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_commit_exactly_one_attempt (market.tests.test_gate7a_acquisition_canary.Gate7AConcurrencyTests.test_concurrent_service_starts_commit_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_permit_exactly_one_running_attempt (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_concurrent_service_starts_permit_exactly_one_running_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_yield_one_winner (market.tests.test_gate7b_full_acquisition.Gate7BConcurrencyTests.test_concurrent_service_starts_yield_one_winner)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_connection_role_is_not_a_superuser (market.tests.test_live_observations.LiveEvidenceDatabaseProtectionTests.test_connection_role_is_not_a_superuser)": [
        {
          "kind": "FAIL",
          "cause": "AssertionError: 'on' != 'off'"
        }
      ],
      "test_contract_binds_the_frozen_portable_identity (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_contract_binds_the_frozen_portable_identity)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_contract_requires_the_sealed_registration (market.tests.test_gate1b_data_contract.Gate1BUnsealedTests.test_contract_requires_the_sealed_registration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_contracts_are_append_only (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_contracts_are_append_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_coverage_defects_are_rejected (market.tests.test_gate7c_dataset_registration.Gate7CFailClosedTests.test_coverage_defects_are_rejected)\n4, 5, 6, 7: a missing, extra, substituted or disagreeing row between": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_database_rejects_forged_contract_hash_and_identity (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_database_rejects_forged_contract_hash_and_identity)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_deferred_trigger_is_installed_with_the_reviewed_shape (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_deferred_trigger_is_installed_with_the_reviewed_shape)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restore_0013_behavior (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_dependency_and_empty_reverse_restore_0013_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0014_behavior (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_dependency_and_empty_reverse_restores_exact_0014_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0015_behavior (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_dependency_and_empty_reverse_restores_exact_0015_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0016_behavior (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_dependency_and_empty_reverse_restores_exact_0016_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_direct_multi_table_and_cascade_truncates_fail_without_data_loss (market.tests.test_zzzzzzz_gate7a_migration.Gate7ATruncateGuardTests.test_direct_multi_table_and_cascade_truncates_fail_without_data_loss)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_discovery_terminal_transition_is_unchanged (market.tests.test_gate7b_audit_completeness.DiscoveryTerminalCompatibilityTests.test_discovery_terminal_transition_is_unchanged)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dry_run_is_read_only_and_reports_the_exact_lineage (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_dry_run_is_read_only_and_reports_the_exact_lineage)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dry_run_performs_complete_validation_and_reports_not_ready (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_dry_run_performs_complete_validation_and_reports_not_ready)\nThe dry run reconstructs every count and identity in a rolled-back": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_phase2b_schema_is_reversible_before_acquisition (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_empty_phase2b_schema_is_reversible_before_acquisition)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_and_reapply_succeed (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_empty_reverse_and_reapply_succeed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0018_definitions (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_empty_reverse_restores_exact_0018_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0019_definitions (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_empty_reverse_restores_exact_0019_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0020_definitions (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_empty_reverse_restores_exact_0020_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_prior_definitions (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_empty_reverse_restores_exact_prior_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_the_0023_catalog_byte_for_byte (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_empty_reverse_restores_the_0023_catalog_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_the_exact_0022_definitions (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_empty_reverse_restores_the_exact_0022_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_eventless_terminal_transitions_are_rejected_at_commit (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_eventless_terminal_transitions_are_rejected_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_every_other_v2_chunk_is_rejected (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_every_other_v2_chunk_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_exactly_one_approval_and_registration_row_can_ever_exist_per_plan (market.tests.test_gate5_registration_activation.Gate5InstalledGovernanceTests.test_exactly_one_approval_and_registration_row_can_ever_exist_per_plan)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_failed_attempt_two_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_failed_attempt_two_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_failed_chunk_cannot_be_retried (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_failed_chunk_cannot_be_retried)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_global_hashes_cannot_shortcut_the_reconstruction (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_forged_global_hashes_cannot_shortcut_the_reconstruction)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_lineage_fails_closed (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_forged_lineage_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_plan_contract_hash_fails_closed (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_forged_plan_contract_hash_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forward_preflight_rejects_all_partial_0009_evidence_categories (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_forward_preflight_rejects_all_partial_0009_evidence_categories)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forward_preflight_rejects_unexpected_discovery_run (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_forward_preflight_rejects_unexpected_discovery_run)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_gate5_functions_are_installed_with_the_reviewed_bodies (market.tests.test_gate5_registration_activation.Gate5InstalledGovernanceTests.test_gate5_functions_are_installed_with_the_reviewed_bodies)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "FAIL",
          "cause": "AssertionError: 'e7d028c9a27596a1b9fba65bd7015cb6' != '5ef0117c6a32cca8a81322a7766d8f52'"
        }
      ],
      "test_generic_plans_are_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_generic_plans_are_rejected_at_both_layers)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_generator_pins_the_exact_sealed_coverage (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_governed_generator_pins_the_exact_sealed_coverage)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_quarantine_is_rejected_and_leaves_no_partial_state (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_governed_quarantine_is_rejected_and_leaves_no_partial_state)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_row_deletes_remain_rejected (market.tests.test_zzzzzzz_gate7a_migration.Gate7ATruncateGuardTests.test_governed_row_deletes_remain_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_stale_resolution_records_its_required_evidence (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_governed_stale_resolution_records_its_required_evidence)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_incomplete_canary_inventory_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_incomplete_canary_inventory_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_incomplete_registration_state_is_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_incomplete_registration_state_is_rejected_at_both_layers)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_invalid_price_failure_is_terminal_and_sanitized (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_invalid_price_failure_is_terminal_and_sanitized)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_legacy_rows_receive_honest_markers_and_nothing_is_fabricated (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_legacy_rows_receive_honest_markers_and_nothing_is_fabricated)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_legacy_rows_reject_v2_lineage_and_v2_identity_needs_contract (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_legacy_rows_reject_v2_lineage_and_v2_identity_needs_contract)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_lineage_and_aggregate_forgeries_fail_closed (market.tests.test_gate7c_dataset_registration.Gate7CFailClosedTests.test_lineage_and_aggregate_forgeries_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_malformed_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_malformed_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_manifest_validation_rejects_tampering_before_any_client_use (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_manifest_validation_rejects_tampering_before_any_client_use)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_migration_depends_directly_on_0012 (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_migration_depends_directly_on_0012)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_migration_is_reversible_and_dependency_pinned (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_migration_is_reversible_and_dependency_pinned)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_missing_attempt_two_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_missing_attempt_two_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_missing_plan_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_missing_plan_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_no_attempt_before_the_complete_plan_exists (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_no_attempt_before_the_complete_plan_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_no_duplicate_strategy_version_or_contract (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_no_duplicate_strategy_version_or_contract)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_non_canary_v2_acquisition_fails_closed_without_inventory_mutation (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_non_canary_v2_acquisition_fails_closed_without_inventory_mutation)\nSince migration 0020, only the exact governed canary chunk may": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_nothing_is_sealed_approved_or_registered_by_a_refusal (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_nothing_is_sealed_approved_or_registered_by_a_refusal)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_null_source_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_null_source_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_one_short_cross_instrument_inventory_keeps_gate_8d2_closed (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_one_short_cross_instrument_inventory_keeps_gate_8d2_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_canary_attempt_one_is_permitted (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_only_canary_attempt_one_is_permitted)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_one_discovery_attempt_may_run_globally (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_only_one_discovery_attempt_may_run_globally)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_the_canary_is_permitted_before_any_success (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_only_the_canary_is_permitted_before_any_success)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_the_registration_validator_changes_and_it_restores_exactly (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_only_the_registration_validator_changes_and_it_restores_exactly)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_operation_aware_dataset_trigger_depends_exactly_on_0011 (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_operation_aware_dataset_trigger_depends_exactly_on_0011)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_orm_bulk_and_queryset_bypasses_fail_closed (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_orm_bulk_and_queryset_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_orm_bulk_bypasses_fail_closed (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_orm_bulk_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_dataset_cannot_register_or_reach_s0s1 (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_partial_dataset_cannot_register_or_reach_s0s1)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_gate_8d1_exposes_only_the_unattempted_cross_instrument_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_partial_gate_8d1_exposes_only_the_unattempted_cross_instrument_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_gate_8d2_returns_the_remaining_keys_in_committed_order (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_partial_gate_8d2_returns_the_remaining_keys_in_committed_order)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_prior_installation_is_rejected (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_partial_prior_installation_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_permission_and_active_user_still_required (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_permission_and_active_user_still_required)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_persistence_failure_recovery_commits_evidence_after_rollback (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_persistence_failure_recovery_commits_evidence_after_rollback)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_portable_identity_correction_depends_exactly_on_0010 (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_portable_identity_correction_depends_exactly_on_0010)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_constants_match_live_0013_catalog (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_constants_match_live_0013_catalog)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_lock_blocks_concurrent_historical_insert_until_0012_is_active (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_preflight_lock_blocks_concurrent_historical_insert_until_0012_is_active)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_passes_and_installs_the_successor_objects (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_preflight_passes_and_installs_the_successor_objects)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0013_governance (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_altered_0013_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0014_governance (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_preflight_rejects_altered_0014_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0015_governance (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_preflight_rejects_altered_0015_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0016_governance (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_preflight_rejects_altered_0016_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0017_governance (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_preflight_rejects_altered_0017_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0018_governance (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_preflight_rejects_altered_0018_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0019_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_preflight_rejects_altered_0019_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0020_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_preflight_rejects_altered_0020_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_evidence_governance (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_preflight_rejects_altered_evidence_governance)\nThe battery must cover evidence governance beyond the functions": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_preflight_rejects_altered_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_function_metadata_changes_with_same_body (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_function_metadata_changes_with_same_body)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_missing_0013_governance (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_missing_0013_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_trigger_calling_wrong_function (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_trigger_calling_wrong_function)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_trigger_with_altered_behavior (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_trigger_with_altered_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_provider_failure_is_terminal_sanitized_and_unrepeatable (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_provider_failure_is_terminal_sanitized_and_unrepeatable)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_pure_generator_is_deterministic_and_portable (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_pure_generator_is_deterministic_and_portable)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_quarantined_run_cannot_be_inserted_and_attached_to_an_attempt (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_quarantined_run_cannot_be_inserted_and_attached_to_an_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_and_queryset_bypasses_fail_closed (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_raw_sql_and_queryset_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_cannot_insert_a_successor_plan_without_its_chunks (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_raw_sql_cannot_insert_a_successor_plan_without_its_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_is_refused_by_postgresql_with_its_own_message (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_raw_sql_is_refused_by_postgresql_with_its_own_message)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_permits_only_the_exact_retry_identities (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_raw_sql_permits_only_the_exact_retry_identities)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_readiness_reporting_writes_nothing_and_builds_no_client (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_readiness_reporting_writes_nothing_and_builds_no_client)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_readiness_reports_the_plan_blocked_with_no_eligible_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_readiness_reports_the_plan_blocked_with_no_eligible_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reapplication_is_deterministic (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_reapplication_is_deterministic)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_replacement_approval_registration_sealing_and_bypasses_fail_closed (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_replacement_approval_registration_sealing_and_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_replacement_plan_dataset_and_chunks_reconstruct_in_postgresql (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_replacement_plan_dataset_and_chunks_reconstruct_in_postgresql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_response_validation_rejects_every_drift_shape (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_response_validation_rejects_every_drift_shape)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_resume_skips_only_successes_and_never_failures (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_resume_skips_only_successes_and_never_failures)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_counts_differ (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_counts_differ)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_failure_code_differs (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_failure_code_differs)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_succeeded_with_inventory (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_succeeded_with_inventory)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_extra_issue_category_present (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_extra_issue_category_present)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_is_refused_once_successor_evidence_exists (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_reversal_is_refused_once_successor_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_any_gate4_attempt (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_reversal_prohibited_after_any_gate4_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_replacement_attempt (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_reversal_prohibited_after_replacement_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_retry_attempt (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_reversal_prohibited_after_retry_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_approval_evidence_exists (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_reversal_prohibited_once_approval_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_contract_evidence_exists (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_reversal_prohibited_once_contract_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_gate7b_evidence_exists (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_reversal_prohibited_once_gate7b_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_replacement_acquisition_evidence_exists (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_reversal_prohibited_once_replacement_acquisition_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reverse_is_refused_while_appended_snapshots_share_an_as_of (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_reverse_is_refused_while_appended_snapshots_share_an_as_of)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reverse_is_refused_while_the_observation_ledger_has_rows (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_reverse_is_refused_while_the_observation_ledger_has_rows)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reverse_migration_rejects_historical_dataset_evidence (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_reverse_migration_rejects_historical_dataset_evidence)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_runner_guardrails (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_runner_guardrails)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_safe_reverse_restores_exact_0011_dataset_trigger_set (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_safe_reverse_restores_exact_0011_dataset_trigger_set)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_sealing_and_registration_governance_is_untouched (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_sealing_and_registration_governance_is_untouched)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_service_attempt_one_succeeds_then_no_retry (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_service_attempt_one_succeeds_then_no_retry)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_several_missing_inventories_report_the_exact_bounded_count (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_several_missing_inventories_report_the_exact_bounded_count)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_single_global_running_acquisition (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_single_global_running_acquisition)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_single_scope_evidence_is_rejected_at_commit (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_single_scope_evidence_is_rejected_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_six_first_h1_successes_expose_exactly_the_126_remainder_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_six_first_h1_successes_expose_exactly_the_126_remainder_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_six_valid_first_h1_inventories_open_the_remaining_126 (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_six_valid_first_h1_inventories_open_the_remaining_126)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_stage_classification_matches_the_committed_membership (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_stage_classification_matches_the_committed_membership)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_successor_sealing_approval_and_registration_remain_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_successor_sealing_approval_and_registration_remain_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_approval_registration_sealing_remain_blocked (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_superseded_approval_registration_sealing_remain_blocked)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_plan_writes_remain_rejected (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_superseded_plan_writes_remain_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_v1_plan_remains_rejected (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_superseded_v1_plan_remains_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_supersession_evidence_blocks_reverse (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_supersession_evidence_blocks_reverse)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_evidence_fails_operational_replay_despite_stored_hashes (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_tampered_evidence_fails_operational_replay_despite_stored_hashes)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_observation_fails_inventory_replay_despite_stored_hashes (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_tampered_observation_fails_inventory_replay_despite_stored_hashes)\nSelf-consistent forgery: the stored summary hashes are left intact": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_replacement_lineage_fails_closed (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_tampered_replacement_lineage_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_accepted_profile_builds_no_provider_client_and_writes_nothing (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeReadinessTests.test_the_accepted_profile_builds_no_provider_client_and_writes_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_accepted_v2_plan_is_unaffected (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_the_accepted_v2_plan_is_unaffected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_earlier_stage_outputs_keep_their_established_fields (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_the_earlier_stage_outputs_keep_their_established_fields)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_exact_successor_plan_and_its_132_chunks_commit (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_the_exact_successor_plan_and_its_132_chunks_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_failed_chunk_still_cannot_take_attempt_two (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_the_failed_chunk_still_cannot_take_attempt_two)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_predecessor_plan_still_takes_the_predecessor_path (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_predecessor_plan_still_takes_the_predecessor_path)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_reporting_command_is_read_only_and_builds_no_provider_client (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_the_reporting_command_is_read_only_and_builds_no_provider_client)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_running_and_failed_rules_stay_diagnostically_distinct (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_the_running_and_failed_rules_stay_diagnostically_distinct)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_successor_plan_remains_unsealed_throughout (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_successor_plan_remains_unsealed_throughout)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_wrong_cross_series_report_is_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_wrong_cross_series_report_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_two_concurrent_raw_retries_produce_exactly_one_attempt (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryConcurrencyTests.test_two_concurrent_raw_retries_produce_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_unmaterialized_successor_permits_nothing_and_is_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_unmaterialized_successor_permits_nothing_and_is_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_v1_evidence_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_v1_evidence_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_v2_registration_rejects_missing_or_foreign_candles (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_v2_registration_rejects_missing_or_foreign_candles)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_executes_sequentially_and_respects_the_bound (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_wave_executes_sequentially_and_respects_the_bound)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_execution_order_resume_and_failure_stop (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_wave_execution_order_resume_and_failure_stop)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_runner_dry_run_bounds_and_read_only (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_wave_runner_dry_run_bounds_and_read_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_stops_on_first_failure_and_leaves_later_chunks_untouched (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_wave_stops_on_first_failure_and_leaves_later_chunks_untouched)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wrong_cross_series_hash_is_rejected (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_wrong_cross_series_hash_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wrong_type_foreign_subject_and_conflicting_evidence_are_rejected (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_wrong_type_foreign_subject_and_conflicting_evidence_are_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_zero_attempts_fail_closed_under_0016 (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_zero_attempts_fail_closed_under_0016)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_zero_canary_attempts_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_zero_canary_attempts_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ]
    }
  },
  "final": {
    "tests": 1002,
    "seconds": 401.694,
    "result": "FAILED (failures=2, errors=238)",
    "log_sha256": "67629f2d916825e61e4f791850fc3c9b8558d34f2523fafde9dc46ac73b85b02",
    "identities": {
      "test_a_canary_with_too_few_earlier_observations_blocks_progression (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_canary_with_too_few_earlier_observations_blocks_progression)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_complete_but_wrong_successor_outcome_is_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_complete_but_wrong_successor_outcome_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_complete_plan_permits_nothing_and_reports_successful_completion (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_complete_plan_permits_nothing_and_reports_successful_completion)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_attempt_on_another_plan_does_not_block_the_successor (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_attempt_on_another_plan_does_not_block_the_successor)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_attempt_permits_nothing_and_reports_the_block (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_failed_attempt_permits_nothing_and_reports_the_block)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_canary_blocks_all_progression (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_failed_canary_blocks_all_progression)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_canary_blocks_every_other_successor_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_canary_blocks_every_other_successor_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_cross_instrument_attempt_blocks_the_rest (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_cross_instrument_attempt_blocks_the_rest)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_failed_remainder_attempt_blocks_a_different_remainder_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_failed_remainder_attempt_blocks_a_different_remainder_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_fresh_materialized_plan_exposes_only_the_canary (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_fresh_materialized_plan_exposes_only_the_canary)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_missing_chunk_fails_at_commit (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_missing_chunk_fails_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_partial_successor_marker_is_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_partial_successor_marker_is_refused)\nA row may not wear the successor identity without earning it, and it": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_partial_successor_outcome_is_refused_before_the_aggregates (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_partial_successor_outcome_is_refused_before_the_aggregates)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_predecessor_failure_does_not_alter_successor_readiness (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_predecessor_failure_does_not_alter_successor_readiness)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_predecessor_inventory_does_not_satisfy_successor_completeness (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_predecessor_inventory_does_not_satisfy_successor_completeness)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_quarantined_attempt_is_blocked_and_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_quarantined_attempt_is_blocked_and_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_row_declared_approval_cannot_seal_without_the_reconstruction (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_a_row_declared_approval_cannot_seal_without_the_reconstruction)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_running_attempt_is_never_reported_as_available (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_running_attempt_is_never_reported_as_available)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_second_attempt_is_never_reported_as_eligible (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_a_second_attempt_is_never_reported_as_eligible)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_second_attempt_one_is_impossible (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_second_attempt_one_is_impossible)\n18, 19: attempt 1 is unique per chunk at the database level.": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_succeeded_attempt_does_not_trigger_the_failed_plan_rule (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_a_succeeded_attempt_does_not_trigger_the_failed_plan_rule)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_successor_plan_short_of_its_declared_count_is_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_a_successor_plan_short_of_its_declared_count_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_a_valid_canary_opens_exactly_the_five_cross_instrument_chunks (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_a_valid_canary_opens_exactly_the_five_cross_instrument_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_131_remaining_chunks_permit_attempt_one_only (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_all_131_remaining_chunks_permit_attempt_one_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_remaining_chunks_eligible_and_canary_permanently_closed (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_all_remaining_chunks_eligible_and_canary_permanently_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_all_succeeded_but_one_inventory_missing_is_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_all_succeeded_but_one_inventory_missing_is_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_altered_governance_is_rejected_by_preflight (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_altered_governance_is_rejected_by_preflight)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_an_exhausted_stage_short_of_completion_permits_nothing (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_an_exhausted_stage_short_of_completion_permits_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_an_inventory_bound_to_another_attempt_does_not_satisfy_the_chunk (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_an_inventory_bound_to_another_attempt_does_not_satisfy_the_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_any_plan_evidence_blocks_reverse (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_any_plan_evidence_blocks_reverse)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_adds_no_object_and_changes_only_the_two_bodies (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_application_adds_no_object_and_changes_only_the_two_bodies)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_failure_commits_exactly_two_scope_events (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_application_failure_commits_exactly_two_scope_events)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_application_success_commits_exactly_two_scope_events (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_application_success_commits_exactly_two_scope_events)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_applied_0011_is_upgraded_to_operation_aware_historical_enforcement (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_applied_0011_is_upgraded_to_operation_aware_historical_enforcement)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_artifact_bytes_dry_run_readiness_and_database_inventory_enforcement (market.tests.test_gate7c_dataset_registration.Gate7CRegistrationTests.test_artifact_bytes_dry_run_readiness_and_database_inventory_enforcement)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_one_ledger_matches_the_governing_finding (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_one_ledger_matches_the_governing_finding)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_three_and_retry_are_rejected_after_failure (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_three_and_retry_are_rejected_after_failure)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_three_and_retry_are_rejected_after_success (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_attempt_three_and_retry_are_rejected_after_success)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_and_failed_evidence_are_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_attempt_two_and_failed_evidence_are_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_is_refused_through_the_service_and_raw_sql (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_attempt_two_is_refused_through_the_service_and_raw_sql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_attempt_two_or_wrong_key_rejected_for_fresh_chunks (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_attempt_two_or_wrong_key_rejected_for_fresh_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_audit_evidence_cardinality_enforced_in_postgresql (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_audit_evidence_cardinality_enforced_in_postgresql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_is_permanently_closed (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_canary_is_permanently_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_prerequisite_reconstructs_and_forgeries_fail (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_canary_prerequisite_reconstructs_and_forgeries_fail)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_success_exposes_the_five_cross_instrument_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_canary_success_exposes_the_five_cross_instrument_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_canary_success_prerequisite_is_established (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_canary_success_prerequisite_is_established)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_candle_conflict_quarantine_rolls_back_and_ends_failed (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_candle_conflict_quarantine_rolls_back_and_ends_failed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_checkpoints_contain_no_secrets_or_prices (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_checkpoints_contain_no_secrets_or_prices)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_execute_fails_closed_without_the_complete_state (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_execute_fails_closed_without_the_complete_state)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_rejects_wrong_plan_sha (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_rejects_wrong_plan_sha)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_requires_an_existing_django_approver (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_requires_an_existing_django_approver)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_success_stores_exactly_the_sealed_series_once (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_command_success_stores_exactly_the_sealed_series_once)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_verifies_the_artifact_byte_for_byte (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_verifies_the_artifact_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_command_verifies_the_completion_summary_byte_for_byte (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_command_verifies_the_completion_summary_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_committed_artifacts_reconstruct_and_reject_alteration (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_committed_artifacts_reconstruct_and_reject_alteration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_committed_authorization_artifact_matches_the_governed_pins (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_committed_authorization_artifact_matches_the_governed_pins)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_complete_but_nonmatching_state_is_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_complete_but_nonmatching_state_is_rejected_at_both_layers)\nAll 132 inventories exist, but synthetic observations cannot match": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_complete_coverage_reaches_registration_boundary_unregistered (market.tests.test_gate7b_full_acquisition.Gate7BFullCoverageTests.test_complete_coverage_reaches_registration_boundary_unregistered)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_approval_attempts_serialize_and_leave_nothing (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_concurrent_approval_attempts_serialize_and_leave_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_sql_starts_commit_exactly_one_attempt (market.tests.test_gate7a_acquisition_canary.Gate7AConcurrencyTests.test_concurrent_raw_sql_starts_commit_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_sql_starts_yield_one_winner (market.tests.test_gate7b_full_acquisition.Gate7BConcurrencyTests.test_concurrent_raw_sql_starts_yield_one_winner)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_raw_starts_permit_exactly_one_running_attempt (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_concurrent_raw_starts_permit_exactly_one_running_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_commit_exactly_one_attempt (market.tests.test_gate7a_acquisition_canary.Gate7AConcurrencyTests.test_concurrent_service_starts_commit_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_permit_exactly_one_running_attempt (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_concurrent_service_starts_permit_exactly_one_running_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_concurrent_service_starts_yield_one_winner (market.tests.test_gate7b_full_acquisition.Gate7BConcurrencyTests.test_concurrent_service_starts_yield_one_winner)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_connection_role_is_not_a_superuser (market.tests.test_live_observations.LiveEvidenceDatabaseProtectionTests.test_connection_role_is_not_a_superuser)": [
        {
          "kind": "FAIL",
          "cause": "AssertionError: 'on' != 'off'"
        }
      ],
      "test_contract_binds_the_frozen_portable_identity (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_contract_binds_the_frozen_portable_identity)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_contract_requires_the_sealed_registration (market.tests.test_gate1b_data_contract.Gate1BUnsealedTests.test_contract_requires_the_sealed_registration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_contracts_are_append_only (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_contracts_are_append_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_coverage_defects_are_rejected (market.tests.test_gate7c_dataset_registration.Gate7CFailClosedTests.test_coverage_defects_are_rejected)\n4, 5, 6, 7: a missing, extra, substituted or disagreeing row between": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_database_rejects_forged_contract_hash_and_identity (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_database_rejects_forged_contract_hash_and_identity)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_deferred_trigger_is_installed_with_the_reviewed_shape (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_deferred_trigger_is_installed_with_the_reviewed_shape)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restore_0013_behavior (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_dependency_and_empty_reverse_restore_0013_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0014_behavior (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_dependency_and_empty_reverse_restores_exact_0014_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0015_behavior (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_dependency_and_empty_reverse_restores_exact_0015_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_and_empty_reverse_restores_exact_0016_behavior (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_dependency_and_empty_reverse_restores_exact_0016_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dependency_atomicity_and_no_data_migration (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_dependency_atomicity_and_no_data_migration)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_direct_multi_table_and_cascade_truncates_fail_without_data_loss (market.tests.test_zzzzzzz_gate7a_migration.Gate7ATruncateGuardTests.test_direct_multi_table_and_cascade_truncates_fail_without_data_loss)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_discovery_terminal_transition_is_unchanged (market.tests.test_gate7b_audit_completeness.DiscoveryTerminalCompatibilityTests.test_discovery_terminal_transition_is_unchanged)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dry_run_is_read_only_and_reports_the_exact_lineage (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_dry_run_is_read_only_and_reports_the_exact_lineage)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_dry_run_performs_complete_validation_and_reports_not_ready (market.tests.test_gate5_registration_activation.Gate5CommandTests.test_dry_run_performs_complete_validation_and_reports_not_ready)\nThe dry run reconstructs every count and identity in a rolled-back": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_phase2b_schema_is_reversible_before_acquisition (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_empty_phase2b_schema_is_reversible_before_acquisition)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_and_reapply_succeed (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_empty_reverse_and_reapply_succeed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0018_definitions (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_empty_reverse_restores_exact_0018_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0019_definitions (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_empty_reverse_restores_exact_0019_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_0020_definitions (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_empty_reverse_restores_exact_0020_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_exact_prior_definitions (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_empty_reverse_restores_exact_prior_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_the_0023_catalog_byte_for_byte (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_empty_reverse_restores_the_0023_catalog_byte_for_byte)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_empty_reverse_restores_the_exact_0022_definitions (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_empty_reverse_restores_the_exact_0022_definitions)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_eventless_terminal_transitions_are_rejected_at_commit (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_eventless_terminal_transitions_are_rejected_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_every_other_v2_chunk_is_rejected (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_every_other_v2_chunk_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_exactly_one_approval_and_registration_row_can_ever_exist_per_plan (market.tests.test_gate5_registration_activation.Gate5InstalledGovernanceTests.test_exactly_one_approval_and_registration_row_can_ever_exist_per_plan)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_failed_attempt_two_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_failed_attempt_two_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_failed_chunk_cannot_be_retried (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_failed_chunk_cannot_be_retried)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_global_hashes_cannot_shortcut_the_reconstruction (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_forged_global_hashes_cannot_shortcut_the_reconstruction)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_lineage_fails_closed (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_forged_lineage_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forged_plan_contract_hash_fails_closed (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_forged_plan_contract_hash_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forward_preflight_rejects_all_partial_0009_evidence_categories (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_forward_preflight_rejects_all_partial_0009_evidence_categories)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_forward_preflight_rejects_unexpected_discovery_run (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_forward_preflight_rejects_unexpected_discovery_run)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_gate5_functions_are_installed_with_the_reviewed_bodies (market.tests.test_gate5_registration_activation.Gate5InstalledGovernanceTests.test_gate5_functions_are_installed_with_the_reviewed_bodies)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "FAIL",
          "cause": "AssertionError: 'e7d028c9a27596a1b9fba65bd7015cb6' != '5ef0117c6a32cca8a81322a7766d8f52'"
        }
      ],
      "test_generic_plans_are_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_generic_plans_are_rejected_at_both_layers)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_generator_pins_the_exact_sealed_coverage (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_governed_generator_pins_the_exact_sealed_coverage)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_quarantine_is_rejected_and_leaves_no_partial_state (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_governed_quarantine_is_rejected_and_leaves_no_partial_state)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_row_deletes_remain_rejected (market.tests.test_zzzzzzz_gate7a_migration.Gate7ATruncateGuardTests.test_governed_row_deletes_remain_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_governed_stale_resolution_records_its_required_evidence (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_governed_stale_resolution_records_its_required_evidence)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_incomplete_canary_inventory_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_incomplete_canary_inventory_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_incomplete_registration_state_is_rejected_at_both_layers (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_incomplete_registration_state_is_rejected_at_both_layers)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_invalid_price_failure_is_terminal_and_sanitized (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_invalid_price_failure_is_terminal_and_sanitized)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_legacy_rows_reject_v2_lineage_and_v2_identity_needs_contract (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_legacy_rows_reject_v2_lineage_and_v2_identity_needs_contract)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_lineage_and_aggregate_forgeries_fail_closed (market.tests.test_gate7c_dataset_registration.Gate7CFailClosedTests.test_lineage_and_aggregate_forgeries_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_malformed_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_malformed_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_manifest_validation_rejects_tampering_before_any_client_use (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_manifest_validation_rejects_tampering_before_any_client_use)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_migration_depends_directly_on_0012 (market.tests.test_zz_discovery_migration.ProviderObservedInventoryMigrationTests.test_migration_depends_directly_on_0012)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_missing_attempt_two_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_missing_attempt_two_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_missing_plan_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_missing_plan_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_no_attempt_before_the_complete_plan_exists (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_no_attempt_before_the_complete_plan_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_no_duplicate_strategy_version_or_contract (market.tests.test_gate1b_data_contract.Gate1BContractTests.test_no_duplicate_strategy_version_or_contract)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_non_canary_v2_acquisition_fails_closed_without_inventory_mutation (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_non_canary_v2_acquisition_fails_closed_without_inventory_mutation)\nSince migration 0020, only the exact governed canary chunk may": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_nothing_is_sealed_approved_or_registered_by_a_refusal (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_nothing_is_sealed_approved_or_registered_by_a_refusal)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_null_source_historical_dataset_aborts_0012_without_partial_installation (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_null_source_historical_dataset_aborts_0012_without_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_one_short_cross_instrument_inventory_keeps_gate_8d2_closed (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_one_short_cross_instrument_inventory_keeps_gate_8d2_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_canary_attempt_one_is_permitted (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_only_canary_attempt_one_is_permitted)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_one_discovery_attempt_may_run_globally (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_only_one_discovery_attempt_may_run_globally)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_the_canary_is_permitted_before_any_success (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_only_the_canary_is_permitted_before_any_success)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_only_the_registration_validator_changes_and_it_restores_exactly (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_only_the_registration_validator_changes_and_it_restores_exactly)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_operation_aware_dataset_trigger_depends_exactly_on_0011 (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_operation_aware_dataset_trigger_depends_exactly_on_0011)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_orm_bulk_and_queryset_bypasses_fail_closed (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_orm_bulk_and_queryset_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_orm_bulk_bypasses_fail_closed (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_orm_bulk_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_dataset_cannot_register_or_reach_s0s1 (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_partial_dataset_cannot_register_or_reach_s0s1)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_gate_8d1_exposes_only_the_unattempted_cross_instrument_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_partial_gate_8d1_exposes_only_the_unattempted_cross_instrument_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_gate_8d2_returns_the_remaining_keys_in_committed_order (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_partial_gate_8d2_returns_the_remaining_keys_in_committed_order)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_partial_prior_installation_is_rejected (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_partial_prior_installation_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_permission_and_active_user_still_required (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_permission_and_active_user_still_required)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_persistence_failure_recovery_commits_evidence_after_rollback (market.tests.test_gate7b_audit_completeness.GovernedTerminalEvidenceTests.test_persistence_failure_recovery_commits_evidence_after_rollback)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_portable_identity_correction_depends_exactly_on_0010 (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_portable_identity_correction_depends_exactly_on_0010)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_constants_match_live_0013_catalog (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_constants_match_live_0013_catalog)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_lock_blocks_concurrent_historical_insert_until_0012_is_active (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_preflight_lock_blocks_concurrent_historical_insert_until_0012_is_active)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_passes_and_installs_the_successor_objects (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_preflight_passes_and_installs_the_successor_objects)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0013_governance (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_altered_0013_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0014_governance (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_preflight_rejects_altered_0014_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0015_governance (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_preflight_rejects_altered_0015_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0016_governance (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_preflight_rejects_altered_0016_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0017_governance (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_preflight_rejects_altered_0017_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0018_governance (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_preflight_rejects_altered_0018_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0019_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_preflight_rejects_altered_0019_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_0020_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_preflight_rejects_altered_0020_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_evidence_governance (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_preflight_rejects_altered_evidence_governance)\nThe battery must cover evidence governance beyond the functions": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_altered_governance_and_partial_installation (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_preflight_rejects_altered_governance_and_partial_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_function_metadata_changes_with_same_body (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_function_metadata_changes_with_same_body)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_missing_0013_governance (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_missing_0013_governance)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_partial_prior_installation (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_preflight_rejects_partial_prior_installation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_trigger_calling_wrong_function (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_trigger_calling_wrong_function)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_preflight_rejects_trigger_with_altered_behavior (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_preflight_rejects_trigger_with_altered_behavior)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_provider_failure_is_terminal_sanitized_and_unrepeatable (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_provider_failure_is_terminal_sanitized_and_unrepeatable)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_pure_generator_is_deterministic_and_portable (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_pure_generator_is_deterministic_and_portable)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_quarantined_run_cannot_be_inserted_and_attached_to_an_attempt (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_quarantined_run_cannot_be_inserted_and_attached_to_an_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_and_queryset_bypasses_fail_closed (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_raw_sql_and_queryset_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_cannot_insert_a_successor_plan_without_its_chunks (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_raw_sql_cannot_insert_a_successor_plan_without_its_chunks)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_is_refused_by_postgresql_with_its_own_message (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_raw_sql_is_refused_by_postgresql_with_its_own_message)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_raw_sql_permits_only_the_exact_retry_identities (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_raw_sql_permits_only_the_exact_retry_identities)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_readiness_reporting_writes_nothing_and_builds_no_client (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_readiness_reporting_writes_nothing_and_builds_no_client)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_readiness_reports_the_plan_blocked_with_no_eligible_chunk (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_readiness_reports_the_plan_blocked_with_no_eligible_chunk)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reapplication_is_deterministic (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeCatalogTests.test_reapplication_is_deterministic)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_replacement_approval_registration_sealing_and_bypasses_fail_closed (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_replacement_approval_registration_sealing_and_bypasses_fail_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_replacement_plan_dataset_and_chunks_reconstruct_in_postgresql (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_replacement_plan_dataset_and_chunks_reconstruct_in_postgresql)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_response_validation_rejects_every_drift_shape (market.tests.test_gate7a_acquisition_canary.Gate7AActivationTests.test_response_validation_rejects_every_drift_shape)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_resume_skips_only_successes_and_never_failures (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_resume_skips_only_successes_and_never_failures)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_counts_differ (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_counts_differ)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_failure_code_differs (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_failure_code_differs)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_attempt_one_succeeded_with_inventory (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_attempt_one_succeeded_with_inventory)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_retry_is_rejected_when_extra_issue_category_present (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_retry_is_rejected_when_extra_issue_category_present)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_is_refused_once_successor_evidence_exists (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_reversal_is_refused_once_successor_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_any_gate4_attempt (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_reversal_prohibited_after_any_gate4_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_replacement_attempt (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_reversal_prohibited_after_replacement_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_after_retry_attempt (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_reversal_prohibited_after_retry_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_approval_evidence_exists (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_reversal_prohibited_once_approval_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_contract_evidence_exists (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_reversal_prohibited_once_contract_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_gate7b_evidence_exists (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_reversal_prohibited_once_gate7b_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reversal_prohibited_once_replacement_acquisition_evidence_exists (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_reversal_prohibited_once_replacement_acquisition_evidence_exists)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_reverse_migration_rejects_historical_dataset_evidence (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_reverse_migration_rejects_historical_dataset_evidence)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_runner_guardrails (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_runner_guardrails)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_safe_reverse_restores_exact_0011_dataset_trigger_set (market.tests.test_migrations.Phase2BMigrationSafetyTests.test_safe_reverse_restores_exact_0011_dataset_trigger_set)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_sealing_and_registration_governance_is_untouched (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeMigrationTests.test_sealing_and_registration_governance_is_untouched)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_service_attempt_one_succeeds_then_no_retry (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_service_attempt_one_succeeds_then_no_retry)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_several_missing_inventories_report_the_exact_bounded_count (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_several_missing_inventories_report_the_exact_bounded_count)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_single_global_running_acquisition (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_single_global_running_acquisition)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_single_scope_evidence_is_rejected_at_commit (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_single_scope_evidence_is_rejected_at_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_six_first_h1_successes_expose_exactly_the_126_remainder_keys (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_six_first_h1_successes_expose_exactly_the_126_remainder_keys)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_six_valid_first_h1_inventories_open_the_remaining_126 (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_six_valid_first_h1_inventories_open_the_remaining_126)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_stage_classification_matches_the_committed_membership (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_stage_classification_matches_the_committed_membership)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzz_replacement_canary_activation_migration.ReplacementCanaryActivationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzz_h1_alignment_retry_migration.H1AlignmentRetryMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzz_full_discovery_migration.FullDiscoveryActivationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzz_gate5_registration_migration.Gate5RegistrationMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate1b_migration.Gate1BMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7a_migration.Gate7AMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7b_migration.Gate7BMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_state_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzzzzzz_gate7c_migration.Gate7CMigrationTests.test_state_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_successor_sealing_approval_and_registration_remain_refused (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeStagedExecutionTests.test_successor_sealing_approval_and_registration_remain_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_approval_registration_sealing_remain_blocked (market.tests.test_gate4_full_discovery.Gate4ActivationTests.test_superseded_approval_registration_sealing_remain_blocked)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_plan_writes_remain_rejected (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryTests.test_superseded_plan_writes_remain_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_superseded_v1_plan_remains_rejected (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_superseded_v1_plan_remains_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_supersession_evidence_blocks_reverse (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_supersession_evidence_blocks_reverse)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_evidence_fails_operational_replay_despite_stored_hashes (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_tampered_evidence_fails_operational_replay_despite_stored_hashes)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_observation_fails_inventory_replay_despite_stored_hashes (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_tampered_observation_fails_inventory_replay_despite_stored_hashes)\nSelf-consistent forgery: the stored summary hashes are left intact": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_tampered_replacement_lineage_fails_closed (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_tampered_replacement_lineage_fails_closed)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_accepted_profile_builds_no_provider_client_and_writes_nothing (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeReadinessTests.test_the_accepted_profile_builds_no_provider_client_and_writes_nothing)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_accepted_v2_plan_is_unaffected (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_the_accepted_v2_plan_is_unaffected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_earlier_stage_outputs_keep_their_established_fields (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_the_earlier_stage_outputs_keep_their_established_fields)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_exact_successor_plan_and_its_132_chunks_commit (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimePlanTests.test_the_exact_successor_plan_and_its_132_chunks_commit)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_failed_chunk_still_cannot_take_attempt_two (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_the_failed_chunk_still_cannot_take_attempt_two)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_predecessor_plan_still_takes_the_predecessor_path (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_predecessor_plan_still_takes_the_predecessor_path)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_reporting_command_is_read_only_and_builds_no_provider_client (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_the_reporting_command_is_read_only_and_builds_no_provider_client)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_running_and_failed_rules_stay_diagnostically_distinct (market.tests.test_gate8b_prime_successor_activation.Gate8bPrimeFailedPlanTests.test_the_running_and_failed_rules_stay_diagnostically_distinct)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_successor_plan_remains_unsealed_throughout (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_successor_plan_remains_unsealed_throughout)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_the_wrong_cross_series_report_is_refused (market.tests.test_gate8d3_prime_registration_authority.Gate8d3PrimeAdmissionTests.test_the_wrong_cross_series_report_is_refused)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_two_concurrent_raw_retries_produce_exactly_one_attempt (market.tests.test_replacement_canary_activation.ReplacementCanaryRetryConcurrencyTests.test_two_concurrent_raw_retries_produce_exactly_one_attempt)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_unmaterialized_successor_permits_nothing_and_is_not_complete (market.tests.test_gate8d2_readiness_correction.Gate8d2ReadinessCorrectionTests.test_unmaterialized_successor_permits_nothing_and_is_not_complete)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_v1_evidence_is_byte_identical_across_empty_reverse_and_reapply (market.tests.test_zzz_discovery_supersession_migration.HistoricalDiscoverySupersessionMigrationTests.test_v1_evidence_is_byte_identical_across_empty_reverse_and_reapply)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_v2_registration_rejects_missing_or_foreign_candles (market.tests.test_gate1b_data_contract.Gate1BGeneratorTests.test_v2_registration_rejects_missing_or_foreign_candles)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_executes_sequentially_and_respects_the_bound (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_wave_executes_sequentially_and_respects_the_bound)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_execution_order_resume_and_failure_stop (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_wave_execution_order_resume_and_failure_stop)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_runner_dry_run_bounds_and_read_only (market.tests.test_gate7b_full_acquisition.Gate7BArtifactTests.test_wave_runner_dry_run_bounds_and_read_only)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wave_stops_on_first_failure_and_leaves_later_chunks_untouched (market.tests.test_gate4_full_discovery.Gate4WaveRunnerTests.test_wave_stops_on_first_failure_and_leaves_later_chunks_untouched)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wrong_cross_series_hash_is_rejected (market.tests.test_gate5_registration_activation.Gate5ApprovalRejectionTests.test_wrong_cross_series_hash_is_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_wrong_type_foreign_subject_and_conflicting_evidence_are_rejected (market.tests.test_gate7b_audit_completeness.DeferredAuditEvidenceTests.test_wrong_type_foreign_subject_and_conflicting_evidence_are_rejected)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_zero_attempts_fail_closed_under_0016 (market.tests.test_replacement_canary_activation.ReplacementCanaryLedgerGateTests.test_zero_attempts_fail_closed_under_0016)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ],
      "test_zero_canary_attempts_blocks_activation (market.tests.test_gate4_full_discovery.Gate4PrerequisiteTests.test_zero_canary_attempts_blocks_activation)": [
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        },
        {
          "kind": "ERROR",
          "cause": "django.db.migrations.exceptions.IrreversibleError: Operation <RunSQL \"\\n-- Mirrors paper._entry_fill's inclusive, execution-side entry predicate.\\nCREATE FUNCTION phase3_observable_activation(rec_id bigint, available_at timestamptz) RETURNS boolean AS $$\\n SELECT EXISTS(\\n SELECT 1 FROM forecasts_recommendation rec\\n JOIN forecasts_targetoccurrence t ON t.id=rec.target_occurrence_id\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=rec.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE rec.id=rec_id AND c.granularity='H1' AND c.complete\\n AND c.timestamp>=rec.generated_at AND c.timestamp+interval '1 hour'<=available_at\\n AND c.timestamp+interval '1 hour'<=((phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'\\n AND run.status='succeeded' AND run.finished_at<=available_at\\n AND CASE WHEN rec.entry_condition='at_or_below'\\n THEN (CASE WHEN rec.action='buy' THEN c.ask_low ELSE c.bid_low END)<=rec.entry_level\\n ELSE (CASE WHEN rec.action='buy' THEN c.ask_high ELSE c.bid_high END)>=rec.entry_level END);\\n$$ LANGUAGE sql STABLE;\\n\\nCREATE FUNCTION phase3_nonactivation_observation() RETURNS trigger AS $$\\nDECLARE rec forecasts_recommendation%ROWTYPE; cutoff timestamptz;\\nBEGIN\\n SELECT * INTO STRICT rec FROM forecasts_recommendation WHERE id=NEW.recommendation_id;\\n IF rec.contract_version<>4 THEN RETURN NEW; END IF;\\n IF TG_TABLE_NAME='forecasts_papertraderesult' THEN\\n  IF NEW.outcome<>'not_activated' THEN RETURN NEW; END IF;\\n  cutoff:=NEW.resolved_at;\\n ELSE\\n  IF NEW.state<>'expired_not_activated' THEN RETURN NEW; END IF;\\n  SELECT resolved_at INTO cutoff FROM forecasts_papertraderesult\\n  WHERE id=NEW.source_id AND recommendation_id=rec.id AND outcome='not_activated';\\n END IF;\\n IF phase3_observable_activation(rec.id,cutoff)\\n THEN RAISE EXCEPTION 'nonactivation_contradicts_entry_evidence'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_papertraderesult\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\nCREATE TRIGGER phase3_nonactivation_observation BEFORE INSERT ON forecasts_recommendationlifecycleevent\\nFOR EACH ROW EXECUTE FUNCTION phase3_nonactivation_observation();\\n\\nCREATE FUNCTION phase3_missing_observation() RETURNS trigger AS $$\\nBEGIN\\n IF NEW.outcome='missing' AND EXISTS(\\n SELECT 1 FROM forecasts_targetoccurrence t\\n JOIN market_candle reference ON reference.id=t.reference_candle_id\\n JOIN market_candle c ON c.instrument_id=t.instrument_id\\n JOIN market_ingestionrun run ON run.id=c.ingestion_run_id\\n WHERE t.id=NEW.target_id AND c.granularity='D' AND c.complete\\n AND c.timestamp=phase3_endpoint(reference.timestamp,t.horizon_sessions)\\n AND ((c.timestamp AT TIME ZONE 'America/New_York')+interval '1 day') AT TIME ZONE 'America/New_York'<=NEW.resolved_at\\n AND run.status='succeeded' AND run.finished_at<=NEW.resolved_at)\\n THEN RAISE EXCEPTION 'available_endpoint_marked_missing'; END IF;\\n RETURN NEW;\\nEND; $$ LANGUAGE plpgsql;\\nCREATE TRIGGER phase3_z_missing_observation BEFORE INSERT ON forecasts_targetresolution\\nFOR EACH ROW EXECUTE FUNCTION phase3_missing_observation();\\n\"> in forecasts.0031_phase3_observable_evidence is not reversible"
        }
      ]
    }
  },
  "new_failures": [],
  "removed_failures": [
    "test_0029_applies_over_legacy_and_cross_source_chains_and_renumbers_dense (market.tests.test_zzzzzzzz_live_observation_migration.LineageRenumberMigrationTests.test_0029_applies_over_legacy_and_cross_source_chains_and_renumbers_dense)",
    "test_0029_migration_depends_on_0028 (market.tests.test_zzzzzzzz_live_observation_migration.LineageRenumberMigrationTests.test_0029_migration_depends_on_0028)",
    "test_legacy_rows_receive_honest_markers_and_nothing_is_fabricated (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_legacy_rows_receive_honest_markers_and_nothing_is_fabricated)",
    "test_migration_is_reversible_and_dependency_pinned (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_migration_is_reversible_and_dependency_pinned)",
    "test_reverse_is_refused_while_appended_snapshots_share_an_as_of (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_reverse_is_refused_while_appended_snapshots_share_an_as_of)",
    "test_reverse_is_refused_while_the_observation_ledger_has_rows (market.tests.test_zzzzzzzz_live_observation_migration.LiveObservationMigrationTests.test_reverse_is_refused_while_the_observation_ledger_has_rows)"
  ],
  "changed_causes_or_multiplicity": {}
}
