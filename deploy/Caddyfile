{
    email {$ACME_EMAIL}
}

{$PUBLIC_HOST} {
    encode zstd gzip
    header {
        -Server
        X-Content-Type-Options nosniff
        Referrer-Policy same-origin
    }
    reverse_proxy web:8000
}
