#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

printf '[setup] trade-recommender: no project dependencies are declared yet\n'
printf '[setup] ready\n'
